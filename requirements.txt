# ============================================================
# Python 依赖包列表
# 安装命令：pip install -r requirements.txt
# ============================================================

# Flask - 核心 Web 框架
Flask==3.1.0

# Flask-SQLAlchemy - 数据库 ORM（让你用 Python 代码操作数据库，不用写 SQL）
Flask-SQLAlchemy==3.1.1

# Flask-Login - 用户登录会话管理
Flask-Login==0.6.3

# PyMySQL - Python 连接 MySQL 数据库的驱动
PyMySQL==1.1.1

# cryptography - PyMySQL 连接加密需要
cryptography==44.0.0

# Werkzeug - Flask 的底层库（密码加密等功能）
Werkzeug==3.1.3

# Flask-Migrate - 数据库迁移工具（管理表结构变更）
Flask-Migrate==4.1.0

# Gunicorn - 生产环境 WSGI 服务器（部署到云服务器时用）
gunicorn==23.0.0

# python-dotenv - 从 .env 文件加载环境变量（密钥、数据库密码等）
python-dotenv==1.0.1

# Flask-WTF - CSRF 防护
Flask-WTF==1.2.2

# openpyxl - Excel 文件生成（导出功能）
openpyxl==3.1.2
